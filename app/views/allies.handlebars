{{!-- this is the file where we will be making a form that takes in the data of whatever eas out and stores it in the tabel
--}} {{!-- make the form --}}

<form id="form" method="POST" action="/allies">
    <div class="row">
        <div class="col">
            <label for="exampleFormControlInput3">Name: </label>
            <input name="name" id="name" type="text" class="form-control" placeholder="Name">
        </div>
    </div>

    {{!-- NAME END --}} {{!-- PHONE START --}}

    <div class="row">
        <div class="col">
            <label for="exampleFormControlInput2">Phone Number: </label>
            <input name="phone" id="phone" type="Phone Number" class="form-control" placeholder="Enter Phone Number">
        </div>
    </div>

    {{!-- PHONE END --}} {{!-- EMAIL --}}

    <div class="form-group">
        <label for="exampleFormControlInput3">Email Address: </label>
        <input name="email" id="email" type="email" class="form-control" id="exampleFormControlInput1" placeholder="name@example.com">
    </div>

    <button id="submit" type="button" class="btn btn-danger">Submit Friend</button>

</form>

<div id="tableContainer">
    <h2> Your Allies </h2>
    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Email</th>
                <th>Phone Number</th>
            </tr>
        </thead>
        <tbody id="new-allies">
        </tbody>
    </table>
</div>

<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
<script>
    $("#submit").on("click", function (event) {
        event.preventDefault();

        var newAllyObj = {
            name: $("#name").val(),
            phone: $("#phone").val(),
            email: $("#email").val()
        };
        console.log(newAllyObj);
        $.post("/api/allies", newAllyObj, function (data) {
            var dataObj = JSON.stringify(data);
            //sent to ther data to database
            for (var i = 0; i < data.length; i++) {
                var allyName = data[i].ally_name;
                var allyPhone = data[i].phone_number;
                var allyEmail = data[i].email_add;
                console.log(allyName);
                console.log(allyPhone);
                console.log(allyEmail);

                $("#tableContainer").append(
                    "<tr class='allies'><td scope='ally-name'-> " + allyName +
                    "</td><td scope='ally-phone'> " + allyPhone +
                    "</td><td scope='start date'> " + allyEmail + " </td></tr>");
            }
            // make div show up at bottom that shows the poeple tht they have added to their friends and things
            // connect to exported function that is in the3 api route
            console.log("data" + dataObj);

        })
    });
</script> {{!-- END EMAIL --}} {{!-- do a value thing and set the variable of value in the form to thew same one --}} {{!-- equal
the values and the things that you eill be inserting but equal them like {{whatevs}}--}}